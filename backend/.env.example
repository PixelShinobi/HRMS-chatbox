# MongoDB Configuration
# Local MongoDB
MONGODB_URI=mongodb://localhost:27017/
DB_NAME=HRWIKI

# Authentication (Legacy - password only, defaults to hr_lead role)
CHATBOT_PASSWORD=hr2025

# User Configuration (RBAC - Role-Based Access Control)
# Format: JSON string with username: {password, role}
# Roles: "hr_lead" (full access) or "hr_junior" (restricted access)
#
# hr_lead: Full access to all data including:
#   - Individual employee salaries
#   - Specific visa expiration dates
#   - Termination records
#   - All benefits and policies
#
# hr_junior: Restricted access to:
#   - Benefits information (health, dental, vision)
#   - Company policies (PTO, sick days, holidays)
#   - Aggregate statistics
#   - General employee info (without salary)
#
HR_USERS={"alice": {"password": "lead123", "role": "hr_lead"}, "bob": {"password": "junior456", "role": "hr_junior"}}

# Ollama LLM Configuration
OLLAMA_MODEL=deepseek-r1:8b
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=2000

# Server Configuration
HOST=0.0.0.0
PORT=8000

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173
